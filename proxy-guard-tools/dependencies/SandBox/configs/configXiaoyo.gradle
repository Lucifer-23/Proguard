gradle.ext {
    // 虚拟机APP版本号
    VERSION_CODE = 1
    // 虚拟机APP版本名
    VERSION_NAME = "1.0.0"

    // 虚拟机APP包名
    APPLICATION_ID = 'com.kun.smallyoo'

    MANIFESTPLACEHOLDERS = [
            OPEN_INSTALL_APPKEY: "hmxgte",// 虚拟机APP对应的openInstall key(华为杀毒引擎会识别此key)
            JPUSH_APPKEY       : "7fe70a9fddddfd41fcc9", // 虚拟机APP对应的Appkey
            JPUSH_CHANNEL      : "developer" // 暂时填写默认值即可.
    ]

    // 虚拟机APP对应的openInstall key(华为杀毒引擎会识别此key)
    OPENINSTALL = "\"hmxgte\""

    // 原生APP加密文件名
    PLAYER = "\"start.mp3\""
    // 原生APP包名
    HOST = "\"com.helloworld.xiaoyo\""

    // 虚拟机APP签名相关
    STORE_FILE = 'configs/sign/xiaoyo.jks'
    STORE_PASSWORD = '654321'
    KEY_ALIAS = 'smallyoo'
    KEY_PASSWORD = '123456'
}