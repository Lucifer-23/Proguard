gradle.ext {
    //版本号
    VERSION_CODE = 93
    //版本名
    VERSION_NAME = "2.3.2"
    // 应用程序包名
    APPLICATION_ID = 'com.wechats.strawberry'

    MANIFESTPLACEHOLDERS = [
            OPEN_INSTALL_APPKEY: "adddd",// 虚拟机APP对应的openInstall key(华为杀毒引擎会识别此key)
            JPUSH_APPKEY       : "7fe70a9fddddfd41fcc9", // 虚拟机APP对应的Appkey
            JPUSH_CHANNEL      : "developer" // 暂时填写默认值即可.
    ]

    // 虚拟机APP对应的openInstall key(华为杀毒引擎会识别此key)
    OPENINSTALL = "\"f7fk26\""

    //版本号
    PLAYER = "\"start.mp3\""
    //包名
    HOST = "\"com.hello.strawberry\""

    // 签名相关
    STORE_FILE = 'configs/sign/strawberry.jks'
    STORE_PASSWORD = '111111'
    KEY_ALIAS = 'key0'
    KEY_PASSWORD = '111111'
}